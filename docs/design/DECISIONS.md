# Decisions log — the review surface

> The design loop runs **autonomously**: when it hits a fork, it picks the best
> production default, proceeds, and records the choice here with its rationale and the
> alternatives it rejected. **This is what you review at the end.** A decision needing
> explicit sign-off (a cost, a vendor lock-in, a security/privacy posture, an
> irreversible product call) is flagged `⚠ confirm`.
>
> One row per decision. Reference a decision from a design doc as `PD<n>`.
>
> **Id namespacing.** This tree's production decisions are **`PDn`** (the rows below).
> Bare **`Dn`** references in the design docs (e.g. `D5`, `D8`, `D11`–`D16`) point to
> the *prototype's existing* design records in
> [`docs/agent-commons.md`](../agent-commons.md),
> [`docs/shared-resource-coordination.md`](../shared-resource-coordination.md), and
> [`docs/capability-broker-architecture.md`](../capability-broker-architecture.md) — a
> separate series, not the rows here. Keeping them distinct is deliberate.

| id | Area | Decision (the default chosen) | Rationale | Alternatives rejected | Flag |
|----|------|-------------------------------|-----------|-----------------------|------|
| PD1 | F1 data model | Relational DB as the system of record for entities + the relation graph (Postgres on web; embedded SQLite on the desktop sidecar). | Entities have clear keys + invariants; the relation graph is a set of typed edges = join tables; transactions give consistent cross-entity ops. Same schema both deployments. | Document store (weak cross-entity integrity); dedicated graph DB (overkill for ≤10 fixed edge types, adds ops burden). | — |
| PD2 | F1 relation graph | Store each relationship as a typed edge (FK column or join table); keep the pure `applyGraphOp` reducer (`contract/graph.ts`) as the client projection + op validator, and apply confirmed ops as a server transaction. The contract `RelationGraph` becomes a read projection. | Normalizes the prototype's seed+edits blob; preserves type-identity (PORT-1) and the consent flow while gaining FK integrity + concurrent-edit safety. | Persist `RelationGraph` JSON as-is (no integrity, lost-update races); event-sourced graph (more than needed now). | — |
| PD3 | F1 identity | ULID `id` + `tenant_id` + timestamps on every row; desktop = one implicit tenant, web = real orgs/users (F2). | Sortable ids; uniform tenant scoping enables the F2 isolation wall; one schema serves both deployments. | Auto-increment ints (leak counts, poor for distributed/merge); UUIDv4 (unsortable). | — |
| PD4 | F1 messages/content | Messages append-only with server `seq`; large message bodies + tool I/O + artifact bodies in object storage via a `*_ref` + digest. | Conversations grow unbounded; keeps rows small + list queries fast; object storage is the right tier for blobs. | Everything inline in the DB (row bloat, slow lists, blob limits). | — |
| PD5 | F1 durable-vs-derived | System of record = sessions/messages/projects/artifacts/schedules+runs/contexts/relation edges/agent-commons registries/audit. Derived (rebuildable, not persisted) = session workspace projection, caps, fs catalogs, usage windows, reservations, live runner registry. | Mirrors the prototype's deliberate persist/transient split (`PersistedState`); lets derived views be cached/recomputed without risking truth. | Persist everything (stale locks/registries outliving the process — exactly what the prototype avoids). | — |
| PD6 | F1 migrations | Forward-only, ordered schema migrations replace the prototype's `STORE_VERSION` discard-and-reseed; `server/data/*` fixtures become dev-only idempotent seeds run through the real write path. | Production can't drop data on a version bump; seeds via the real mutators can't drift (same idea as `snapshot:build`). | Keep version-mismatch → reseed (data loss); hand-edit prod data (unsafe). | — |
| PD7 | F1 lifecycle | Soft-delete (tombstones) for user entities; audit entries immutable + append-only; stale references resolve to a "no longer available" state. | Preserves the audit trail + consent history; matches the prototype's graceful handling of stale ids; supports undo. | Hard delete (breaks audit + dangling refs); no delete (clutter). | — |
| PD8 | F2 authn | Web: OIDC/OAuth 2.0 → short-lived access + refresh tokens (httpOnly cookie or bearer). Desktop: no remote auth — loopback sidecar trusts the local OS user, gated by a per-launch loopback token. | Standard, SSO-friendly web login; desktop stays local-first/offline (INV-3 loopback) without inventing auth; one contract serves both. | Username/password (worse security, no SSO); making the desktop require login (breaks local-first). | ⚠ confirm (web IdP choice) |
| PD9 | F2 tenancy | Org = the tenant boundary; `tenant_id` on every row; isolation enforced at the **data layer** (Postgres RLS or a mandatory repository scope), default-deny; SSE channels, caches, runner registry, and guardian all tenant-partitioned. | A forgotten per-call filter can't leak if isolation is below the handlers; one shared DB with RLS is the standard multi-tenant SaaS posture. | Per-tenant database/schema (heavier ops, harder cross-tenant ops); app-layer-only filtering (one bug = a leak). | ⚠ confirm (shared-DB + RLS vs DB-per-tenant) |
| PD10 | F2 contract | Add `GET /v1/me` (user + active org + memberships) and make `client.ts` credentialed; the "one door to the backend" stays one door, now authenticated. | Mirrors how the UI already reads `/capabilities`; minimal contract change; preserves the single-door invariant. | A separate auth SDK/path (breaks one-door); embedding identity in every payload (redundant). | — |
| PD11 | F2 authz | Reuse the D8 authority cascade (`contract/authority.ts`) + `ProjectRole` (`contract/roles.ts`) for resource access; org-membership roles (owner/admin/member) gate only org administration. | The authority/role/coordination primitives already exist and are tested; avoids a parallel RBAC system that could drift. | A brand-new RBAC engine; ACLs per resource (doesn't compose with the existing cascade). | — |
| PD12 | F2/F4 runner identity | A runner is bound to a user/org and enrolled via a scoped, revocable token (not user credentials); its capabilities are tenant-scoped. | Runners act on a host on a tenant's behalf; a scoped token is least-privilege and revocable; keeps the registry tenant-isolated. | User credentials on the runner (over-broad, unrevocable); anonymous runners (no isolation). | — (detailed in F4) |
| PD13 | F3 sync | A pub/sub bus (Redis Streams / NATS / managed equiv.) fans SSE out across API instances on per-`(tenant,user)` topics, with `Last-Event-ID` resume; the desktop keeps the in-process bus it has today. | Multiple API instances must converge a user's devices; the advisory-invalidation model makes a lightweight bus sufficient; desktop is the single-node degenerate case. | Sticky single-instance per user (no HA); polling (latency + load); streaming full state (heavy). | ⚠ confirm (bus technology) |
| PD14 | F3 reads | Cursor (keyset) pagination for unbounded lists (sessions, messages, runs, audit). | Stable under concurrent inserts; bounded payloads; pairs with P1 history virtualization. | Offset pagination (drifts under inserts, slow deep pages); unbounded lists (the prototype's shape — won't scale). | — |
| PD15 | F3 writes | Generalize the invoke path's `commandId` idempotency key to **all** mutations; pair with optimistic-concurrency `409`s (PD2). | Retries/reconnects must not double-apply; the mechanism already exists for invokes. | At-most-once with no key (lost writes on retry); server-side dedup heuristics (fragile). | — |
| PD16 | F3 sync model | Keep events **advisory** ("what changed" → client refetches), enabling coalescing/drop under backpressure; do not stream authoritative state diffs over SSE. | Coalesce/drop-safe is what makes fan-out + backpressure tractable; already the prototype's model (`events.ts`). | CRDT/state-diff streaming (complex, overkill for this read-heavy UI). | — |
| PD17 | F3 contract evolution | Additive-only within `/v1`, gated by `Capabilities` flags; a breaking change is a new version segment with a deprecation window. | Preserves contract type-identity (PORT-1) across both backends; flags already gate native-vs-web. | Mutating `/v1` shapes (breaks clients/portability); no versioning (no safe path to evolve). | — |
| PD18 | F4 transport | Runners dial OUT to the broker over an authenticated persistent stream (WebSocket/gRPC bidi); the broker routes invocations back. | Works behind NAT/firewall with no inbound route to the host; one long-lived auth'd channel; desktop collapses it to an in-process call. | Broker→runner inbound connections (NAT/firewall pain, larger attack surface); polling (latency). | ⚠ confirm (WS vs gRPC) |
| PD19 | F4 topology | Relay-through-broker by default; co-located fast path on desktop (the sidecar's in-process runner). | The broker-doc D1 duality: one model where native vs web differ only by co-location; keeps the contract identical. | Relay-only (loses desktop's local speed/offline); fast-path-only (no web/remote hosts). | — |
| PD20 | F4 journal | The per-runner effect journal is a durable, append-only event log in shared storage, idempotent by `commandId`; the server projection is a rebuildable cache; the outbox/sync path reconciles out-of-band effects. | The runner is the system of record for its host (broker-doc D2); durability + shared storage enable HA/failover; at-least-once with idempotent merge. | In-memory journal (lost on failover — the prototype's transient form); no outbox (fast-path/offline effects lost). | — |
| PD21 | F4 coordination | Reservations/guardian live in shared storage (not per-instance memory), TTL-swept. | Escrow must be correct across a horizontally-scaled API tier and survive failover; TTL reclaims crashed holders. | Per-instance memory (escrow breaks across instances — prototype's form); no TTL (stuck locks). | — |
| PD22 | F4 enforcement | The broker is the reference monitor / policy decision point (mediation → commission → role → reservation), the runner is the policy enforcement point (host grant D3); neither requester nor model holds a host credential. | Layered defense; the structural confused-deputy wall the prototype already encodes; keeps secrets off the requester. | Trusting the runner to self-authorize (no central policy); trusting the broker to enforce host grants (it doesn't run the host). | — |
| PD23 | F5 consent | Server-enforced consent gate for every irreversible/external effect (proposal → confirm); standing approvals are scoped, revocable, audited grants; reads need no consent (CALM). | The prototype's escalation/relation-card model, hardened so the gate isn't just UI; standing grants make recurring effects unprompted-because-pre-authorized. | Auto-execute model tool calls (unsafe); gate reads too (friction with no benefit). | — |
| PD24 | F5 authz | The D8 authority cascade + D12 project-clamp + D14 role is THE authorization for agent-driven effects, enforced server-side at mint AND effect time, with parent-shrink reclamp. | Already designed/tested in the prototype; single decision point; over-grant unrepresentable; composes with F2 human RBAC. | A separate per-effect ACL system (drifts from the cascade); UI-only checks (bypassable). | — |
| PD25 | F5 injection | Untrusted content (files/connectors/web) is provenance-tagged and treated as data, not instructions; capability containment + the consent gate backstop prompt-injection. | A model is untrusted-by-construction; containment means injected text can't grant unheld reach; consent stops unprompted commits. | Trusting model output to be benign; relying on prompt wording alone (brittle). | — |
| PD26 | F5 secrets | Connector tokens + provider keys in a KMS/secrets-manager, per-tenant-encrypted, off the contract, JIT-scoped to the executor, rotated + revocable. | The prototype already keeps provider config server-only; production adds rotation/scoping/revocation; blast radius bounded to one tenant/connector. | Secrets in the DB/contract (exposure); long-lived shared keys (no rotation/revocation). | — |
| PD27 | F5 audit | Tamper-evident, append-only audit of cross-principal + irreversible effects (fulfilled and denied) + consent decisions; tenant-scoped retention/export. | Highest-value security signal (D15 already records denied cross-user effects); supports compliance evidence. | No audit / mutable logs (no forensics, no compliance). | — |
| PD28 | F6 datastore | Postgres (web, with replicas) / embedded SQLite (desktop); forward-only migrations on deploy/update. | One schema, two engines; replicas scale reads + failover; SQLite makes the desktop a single self-contained bundle. | One engine for both (SQLite won't scale web; Postgres is heavy to embed); discard-on-version (data loss). | ⚠ confirm (Postgres choice) |
| PD29 | F6 object storage | S3-compatible object storage (web) / local app-data (desktop), content-addressed, per-tenant prefix, short-lived signed URLs, lifecycle-expired. | Right tier for blobs (bodies, file/photo content, uploads); signed URLs keep bytes off the API and generalize the CTX-FS `<img src>` model. | Blobs in the DB (bloat, limits); public buckets (leak); API-proxied bytes (load). | — |
| PD30 | F6 deployment | Web = stateless autoscaled API tier (serves UI + contract) + managed DB/object-store/bus/secrets + a model gateway; sticky only for the reply stream. Desktop = one bundled sidecar + co-located runner. | Statelessness gives HA + horizontal scale; the reply stream is the only inherently sticky path; desktop stays self-contained/offline. | Stateful API nodes (hard to scale/heal); desktop calling a remote API for everything (loses local-first). | — |
| PD31 | F6 observability | OpenTelemetry logs/metrics/traces (tenant-tagged) + SLOs (availability, p99, generation success, sync lag) with error budgets gating rollouts. | Vendor-neutral instrumentation; SLOs make reliability measurable + gate risky deploys. | Ad-hoc logging (no traces/SLOs); a single-vendor agent (lock-in). | — |
| PD32 | F6 durability | Durable (backed up + PITR) = relational entities/graph/registries/audit + object-storage blobs; transient (rebuildable, not backed up) = reservations, live registry, usage windows, fs catalogs, workspace projection. | Restates PD5 at the storage tier; backups protect truth, transient stores recompute — cheaper + simpler recovery. | Back up everything (stale locks/registries restored — exactly what the prototype avoids). | — |
| PD33 | P1 entry points (FWD-1) | `newSession` takes an optional context seed; "New from repo/folder" are shortcuts that pre-attach via the same funnel and land already-escalated; "New chat" is seed-less. | The old per-mode entries survive as shortcuts, not tabs (PROPOSAL §7); one code path; honors one-door/one-funnel. | Separate per-mode session types (reintroduces modes); a wizard (heavier). | — |
| PD34 | P1 panel state (FWD-2) | Persist per-conversation panel state (open/which/width) as server-side user prefs keyed by `(user, session)`, syncing across devices; `strongestFocus` is the default; `localStorage` fallback offline. | Realizes the §7 "remember per-conversation"; server-side gives the multi-device payoff of server-owned state. | Re-derive every open (today — no memory); `localStorage` only (no sync). | — |
| PD35 | P1 responsive (FWD-3) | A wide/medium/narrow ladder: side-by-side → overlay drawer → collapsed rail for the right panel, via a `useViewport` hook + CSS breakpoints. | Closes the §8 open question; keeps chat usable on small windows; panel stays resizable per tier. | Fixed widths (today — unusable narrow); hide the panel entirely (loses density). | — |
| PD36 | P1 performance | Virtualize the message history (windowed list) + cursor pagination (F3 PD14), newest-first; lazy-load panel/artifact bodies + images. | Long threads must stay bounded in DOM/memory; pairs with the paginated contract. | Render the whole thread (unbounded DOM); offset paging (drifts, F3 PD14). | — |
| PD37 | P1 accessibility | WCAG 2.2 AA baseline: keyboard order + focus management (reuse `useFocusTrap`/`useDismissable`), ARIA landmarks + `aria-live` for streaming, reduced-motion, AA contrast (light-theme, INV-1). | A shipped client must be accessible; the focus primitives already exist. | A11y as an afterthought (costly retrofit); color-only cues (fails AA). | — |
| PD38 | P2 UI-host source | Web: browser File System Access API / `<input>` (`uiHostFs.ts`), bytes client-side, resumable digest-keyed upload to object storage on effect. Desktop: "This computer" = the co-located runner reading real local fs. | A web server can't read the browser's disk (the documented one-door exception); desktop gets full local access via the sidecar fast path. | Always-browser (desktop loses real local fs); always-runner (web can't read the browser). | — |
| PD39 | P2 runner source | Runner fs `list`/`read` over the broker, cursor-paginated, with runner-pushed change events → SSE invalidation; binary via the broker bytes route. | Large dirs must page; watch-driven invalidation keeps an open catalog fresh via the advisory-event model (F3 PD16). | Whole-dir reads (won't scale); poll for changes (latency + load). | — |
| PD40 | P2 cloud source | Cloud storage = a per-tenant object-storage prefix (F6 PD29); listing paginated (prefixes = folders), content via signed URLs, uploads direct-to-store via signed PUT. | Object storage is the right backing; signed URLs keep bytes off the API and generalize the CTX-FS `<img src>` model; bytes skip the API on upload too. | Bytes through the API (load); a separate file DB (reinvents object storage). | — |
| PD41 | P2 listing & mediation | Catalogs paginated + cached + invalidation-driven; id = source-relative path with the resolved-under-root guard; `SessionContext.scope` set to the source-rooted path so mediation (F4 PD22/D5) matches the host grant exactly. | Closes the prototype's latent note that a bare relative scope wouldn't match a `~/projects/...` target; keeps traversal safety. | Absolute paths from the client (traversal/leak); bare relative scope (mediation mismatch). | — |
| PD42 | P2/P5 content | Replace the hand-authored artifact-content library (spec MOCK-2) with real extraction/rendering of the attached file; model-assisted summary/preview where useful (P5). | Makes folder/file content real end-to-end (the goal "only the model is mocked"); removes a mock-beyond-intent surface. | Keep the fixture library (a permanent mock); render raw bytes only (poor preview for rich formats). | — |
| PD43 | P3 execute-vs-propose | Monotonic tool calls execute directly in the loop; irreversible/external ones become consent-gated proposals (the escalation/relation card), executed on confirm or a standing approval. | The prototype's exact model, generalized to all tools; the consent gate is the safety for irreversibility (F5 PD23). | Auto-execute everything (unsafe); propose everything incl. reads (friction). | — |
| PD44 | P3 executors | Map each tool to a real executor: runner (fs/git/terminal — replaces MOCK-4 fixtures), connector framework (connectors/MCP, P6), graph transaction (relations, P4), content extraction (workspace/artifacts, P2). | Swaps mock fulfilment for real backends while keeping the tool-use protocol + consent shape intact. | A monolithic executor (couples concerns); keep fixtures (permanent mock). | — |
| PD45 | P3 rollback | Reversible effects get undo via the soft-delete + object-store version trail (F1 PD7) and a pre-commit "review changes" surface; irreversible effects rely on the consent gate (no undo possible). | Gives safety where undo is feasible and is honest where it isn't (the reason the consent gate exists). | Promise universal undo (impossible for sent email/charges); no review surface (blind commits). | — |
| PD46 | P3 audit | Every executed tool call (observational + irreversible, fulfilled + failed) is audited (F5 PD27) with principal/capability/target/outcome. | Forensics + abuse detection (spike in authority-refused calls = injection signal); extends the prototype's cross-user audit to all effects. | Audit only failures / only writes (blind spots). | — |
| PD47 | P4 graph edits | Confirmed relation ops apply as transactions with optimistic concurrency (base-version check); disjoint edges commute, same-edge conflict → `409` → client re-reads + re-proposes; the pure reducer stays for optimistic projection + validation. | Multi-user safety on the shared graph without locks; preserves the prototype's consent flow + type-identity. | Last-write-wins (silent clobber); pessimistic locks (contention, deadlock risk). | — |
| PD48 | P4 scheduler | A durable scheduler (cron/leader on web, in-process timer on desktop) replaces `setInterval`: runs idempotent by `(schedule, fire_time)`, retried with backoff, timezone-correct, with a configurable missed-fire policy (catch-up vs skip). | Production can't lose runs on restart or double-fire on failover; the contract already carries timezone + notifyOnFailure. | `setInterval` (lost on restart, no HA, no idempotency — the prototype's form). | ⚠ confirm (catch-up vs skip default) |
| PD49 | P4 run execution | A run executes the real model+tool loop (P5/P3) under the schedule's **bound agent authority** (clamped, PD24), delivering via the graph bindings (artifact/session/extra-tools); the routine owns one live artifact refreshed in place. | A scheduled effect must be least-privilege (its agent, not an ambient super-user); in-place refresh keeps the store bounded (the prototype's rule). | Run as a privileged system action (over-broad); append a new artifact per run (unbounded growth). | — |
| PD50 | P4 standing approvals | Standing approvals are scoped, revocable, pre-authorized grants the daemon acts under; every unprompted application is audited; revoking stops future effects. | Realizes "approved once, then runs unprompted" (PROPOSAL §4.7) safely — the grant is the durable consent, withdrawable. | Re-prompt every run (defeats automation); permanent un-revocable grants (unsafe). | — |
| PD51 | P5 model gateway | A server-side model gateway mediates the Anthropic Messages API: keys from KMS, per-agent/tenant token budgets, retries/timeouts/fallback, streaming relay + cancellation; going live from the mock is config only (MOCK-1). | The boundary is already real via the SDK; a gateway adds the operational controls (cost, resilience, audit) at the single egress. | Direct per-request SDK calls with no gateway (no central budget/retry/audit); a different model vendor (off-thesis). | — |
| PD52 | P5 compaction | When context nears the window, summarize older turns via a model call, preserving pinned context, keeping full history in storage; surface the warm caption + progress + gauge drop (docs/context-compaction.md). | Keeps long conversations usable (the natural sequel to the usage gauge); realizes BROKER-EXP-3; history isn't lost (stored). | Hard-truncate oldest silently (data loss, jarring); refuse once full (dead-ends the chat). | — |
| PD53 | P5 budget | Meter real per-turn tokens into the rolling windows and enforce via the D8 cascade per agent/provider/account, plus per-tenant plan caps; refuse over-limit turns with `limit_exceeded` before streaming. | The metering + enforcement seam already exists (`usageMeter`/`overSpendLimit`); pre-stream refusal avoids partial-charge surprises. | No budget enforcement (cost blowout); enforce after streaming (already-spent surprise). | — |
| PD54 | P5 providers | Multi-provider via the D9 abstraction; an Agent binds a `providerId`; the concrete model id + credentials stay server-only config off the contract; the gateway routes to the bound family; the D10 prompt-fit probe scores selection. | Already designed in the prototype; keeps secrets/model-id off the wire (like `Capabilities` hides the key); supports future model diversity. | Hard-code one model (no flexibility); put model id/keys on the contract (leak). | — |
| PD55 | P5 tool loop | Harden the tool-use loop: bounded iterations, per-tool timeouts, structured tool errors fed back, idempotency (F3 PD15), the execute-vs-propose consent split (P3 PD43); tool schema evolves additively (F3 PD17). | Prevents runaway loops/hangs, lets the model recover from tool failures, keeps effects safe + idempotent. | Unbounded loop (cost/hang); swallow tool errors (model can't adapt). | — |
| PD56 | P6 connectors | OAuth2 connector framework via per-provider adapters; tokens in KMS per (tenant,user,connector); `SavedContext.status` driven by the existing `connector.status` callback/expiry seam. | Standard integration auth; the status seam already exists; secrets stay off the contract (F5). | Per-connector bespoke auth (no uniformity); tokens in the DB/contract (leak). | — |
| PD57 | P6 connector tools | Connector resources surface as read tools and writes as authority-gated (PD24) + consent-gated (PD43) tools; the GitHub-connector↔repo dependency is preserved. | Lets the model use connectors safely within the same authority/consent model as every effect. | Ungated connector writes (unsafe); connectors as data-only (loses the actions). | — |
| PD58 | P6 MCP | Real MCP: stdio via the runner on desktop / HTTP+SSE remote; servers sandboxed + scope-bounded (D3); discovered tools appended to the model tool list. | Makes MCP real (removes MOCK-3); the runner already gives a sandbox + scope model; output treated as untrusted (PD25). | Static MCP labels (permanent mock); unsandboxed MCP (third-party code risk). | ⚠ confirm (MCP sandbox policy) |
| PD59 | P6 attach | A connector/MCP attach is a `SessionContext` with an account scope, mediated (D5) + project-clamped (D12), reusable from the Contexts page without re-auth. | Uniform with all context (one attachment-of-record model); reuse avoids re-auth friction. | A separate connector-session model (fragments context); re-auth per use (friction). | — |
| PD60 | P7 jobs | Scheduled (recurring) and Dispatch (one-shot) are one job model on one queue/executor — durable run rows, at-least-once + idempotent, retried with backoff, concurrency-capped per tenant/agent. | Both shapes already exist; one engine avoids divergence; caps protect budgets/runners. | Two separate execution paths (drift); unbounded concurrency (budget/runner exhaustion). | — |
| PD61 | P7 templates | Schedule templates = a curated + user-defined library that seeds new routines through the real create path. | Lowers the bar to automation; a template is just a seed (can't drift from the contract). | Hard-coded templates only (inflexible); no templates (every routine from scratch). | — |
| PD62 | P7 safety | A circuit breaker + budget caps halt a runaway or repeatedly-failing job (disable + alert); no silent infinite spend. | Automation runs unattended — it must fail safe; protects cost + shared resources. | Retry forever (cost/runner blowout); silent disable (operator blind). | — |
| PD63 | P8 commons | Provider/prompt/agent/commission registries are tenant-scoped, managed by hand AND conversationally through one funnel; the D8 cascade enforced at mint/patch/reclamp server-side. | The single funnel keeps both management paths honest (the prototype's design); per-tenant isolation; over-grant unrepresentable. | Two enforcement paths (drift); client-side grant checks (bypassable). | — |
| PD64 | P8 D11–D16 | Enforce the full model in production: guardian sub-goal coordination (D11), Project-clamped reach (D12), roles (D14), commission caps + reputation (D13), cross-principal audit (D15), per-turn provenance (D16). | These are designed + tested in the prototype; production scales them across tenants/users via the shared guardian + audit. | Drop the multi-principal guarantees (the confused-deputy wall is the whole point). | — |
| PD65 | P8 abuse / forward | Abuse controls = per-Project commission caps + budget caps + rate limits + the detective audit trail. Cross-org agent/prompt sharing (a marketplace, cross-org commissions) is a flagged forward extension. | Bounds multi-tenant abuse with existing primitives; cross-org sharing expands the trust boundary + needs product/legal calls. | No caps (abuse); ship cross-org sharing now (unvetted trust/billing/data-sharing). | ⚠ confirm (cross-org sharing scope) |

## Standing assumptions (set at kickoff)

These frame every decision below; called out so the end review can challenge the frame,
not just the leaves:

- **Target = both deployments** (desktop sidecar + multi-tenant web), one shared contract.
- **Design, not build** — engineering-design altitude; the prototype is not converted.
- **Preserve the prototype's locked invariants** where they still apply in production
  (one door to the backend, framework-free contract, consent-gated irreversible effects,
  the model as the generation seam) unless a decision explicitly revisits one (flagged).
- **Cloud vendor-neutral by default** — name a concrete technology only as an example,
  and prefer portable abstractions; any hard dependency is flagged `⚠ confirm`.
