# Scratch

Local notes that live on this host (read through the runner / broker, not the
web backend). Edit me — the change tracks as unsaved.

- The composite index on (cohort, day) cut p95 from 1.8s to 240ms.
- Variant B copy: "Your numbers, finally in one place."
