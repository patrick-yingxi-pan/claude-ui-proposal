# Insights Dashboard

The analytics surface shipping behind the `insights` flag this quarter.

- `routes/insights.tsx` — the page
- `widgets/` — revenue, funnel, retention cards
- Owner: Platform team
