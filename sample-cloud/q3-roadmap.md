# Q3 Roadmap

A real file served from the web backend's cloud storage (`sample-cloud/`), not a
compile-time fixture. Edit it and the change tracks as unsaved in the panel.

## Themes

- **Insights dashboard** — ship behind the `insights` flag, then GA.
- **Unified auth** — collapse the three refresh paths into one.
- **Latency** — hold p95 under 300ms through the launch.

## Milestones

1. Beta to design partners (week 2)
2. Internal GA (week 5)
3. Public GA (week 8)
